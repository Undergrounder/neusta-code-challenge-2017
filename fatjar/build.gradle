group 'de.neusta_sd.roomsmanager.fatjar'
version '1.0-SNAPSHOT'

apply plugin: 'org.springframework.boot'

dependencies{
    compile('org.springframework.boot:spring-boot-starter')
    compile project(':roomsmanager-frontend')
}