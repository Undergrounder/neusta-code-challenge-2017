group 'de.neusta_sd.roomsmanager.frontend'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8
targetCompatibility = 1.8

dependencies{
    compile project(':roomsmanager-facades')
}